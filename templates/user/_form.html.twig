 <div class="flex justify-center mt-10 ml-10">
                <div class="hidden h-screen ml-4 bg-cover bg-center lg:block lg:w-1/3" id="random-image-container"
                     style="clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);">
                    <div class="flex items-center h-full px-20 bg-gray-900 bg-opacity-40">
                        <div>
                            <h2 class="text-4xl font-bold text-white uppercase" style="color: white;font-weight: bold">
                                Edit User</h2>
                        </div>
                    </div>
                </div>
                <!---END OF PICTURE PART----->

                <!-- Login -->

                <div class="flex items-center max-w-md px-6 m-auto">
                    <div class="flex-1">
                        <div class="text-center">
                            <h2 class="text-4xl font-bold text-center text-gray-700 dark:text-white">TRAVEL TIPS</h2>

                            <p class="mt-3 text-gray-500 dark:text-gray-300">Edit your account</p>
                        </div>
                        <div class="mt-8">
                            {{ form_start(form) }}
                            {{ form_widget(form) }}
                            <button class="w-full mt-4 px-4 py-2 tracking-wide text-white transition-colors duration-200 transform bg-gray-900 rounded-md hover:bg-blue-400 focus:outline-none focus:bg-blue-400 focus:ring focus:ring-blue-300 focus:ring-opacity-50">{{ button_label|default('Save') }}</button>
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </div>
<script>
    //Random Picture Register
    // Tableau d'URLs d'images
    const imageUrls = [
        '/build/images/1.jpg',
        '/build/images/2.jpg',
        '/build/images/3.jpg',
        '/build/images/4.jpg',
    ];

    // Générer un nombre aléatoire entre 0 et 3 (pour accéder au tableau)
    const randomNumber = Math.floor(Math.random() * imageUrls.length);

    // Sélectionner l'élément à mettre à jour
    const randomImageContainer = document.getElementById('random-image-container');

    // Appliquer l'URL de l'image aléatoire en tant que background-image
    randomImageContainer.style.backgroundImage = `url(${imageUrls[randomNumber]})`;
</script>
